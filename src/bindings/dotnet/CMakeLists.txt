set(CSHARP_INTERFACES
    csharp_showtime.i
)

SET_SOURCE_FILES_PROPERTIES(${CSHARP_INTERFACES} PROPERTIES CPLUSPLUS ON)

#FIND_PACKAGE(PythonLibs 2.7 REQUIRED)
#INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_PATH} ${BASEPATH})
include (${SWIG_USE_FILE})
set(CMAKE_SWIG_OUTDIR ${CMAKE_CURRENT_BINARY_DIR})

SWIG_ADD_LIBRARY(showtime_csharp LANGUAGE csharp TYPE SHARED SOURCES ${CSHARP_INTERFACES} ${ZST_HEADERS})

SWIG_LINK_LIBRARIES(showtime_csharp 
	${PROJECT_NAME}
	${ZeroMQ_LIBRARIES} 
	${PYTHON_LIBRARIES}
	${CZMQ_LIBRARIES}
	${MSGPACK_LIBRARIES}
)